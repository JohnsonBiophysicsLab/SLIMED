/**
 * @file Patch_kernel.cpp
 * @brief Host instantiation of the patch kernel.
 *
 * The body lives in Patch_kernel.inl so a CUDA translation unit can include
 * the same source and compile it for the device.
 */
#include "energy_force/Patch_kernel.inl"
